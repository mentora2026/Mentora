# Base URL for the backend API (Step 2/3 FastAPI service).
# During local development with `docker compose up` in the backend folder,
# the API is available at http://localhost:8000.
VITE_API_BASE_URL=http://localhost:8000/api/v1
