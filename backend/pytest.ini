[pytest]
markers =
    unit: Pure-logic unit tests with no database dependency (run anywhere, instantly).
    integration: API/database integration tests (require DATABASE_URL pointing to a seeded PostgreSQL instance).

testpaths = tests
